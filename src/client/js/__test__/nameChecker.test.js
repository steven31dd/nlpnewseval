import { checkForName } from "../nameChecker"

describe("Testing the checkForName functionality", () => {
      test("checkForName defined?", () => {
           expect(checkForName).toBeDefined();
})});


checkForName
